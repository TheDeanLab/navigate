.. _issue_histogram_lag:

====================
Histogram Lag in GUI
====================

In high-speed imaging or very large-frame acquisitions, histogram updates can introduce noticeable GUI lag.

To improve responsiveness, disable histogram rendering by right-clicking the histogram and selecting the disable option in the popup menu. You can re-enable it from the same menu when needed.
